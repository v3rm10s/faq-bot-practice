version: 1.0
build:
  image: faq-bot-practice # A name for the resulting container image
  docker:
    dockerfile: Dockerfile # Path to your Dockerfile
run:
  port: 8080 # The port EXPOSEd in your Dockerfile
