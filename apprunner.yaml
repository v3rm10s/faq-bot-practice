version: 1.0
build:
  image: python-app # A name for the resulting container image
  docker:
    dockerfile: Dockerfile # Path to your Dockerfile
run:
  port: 80 # The port EXPOSEd in your Dockerfile
